# Wings API environment
PORT=3001
WEB_ORIGIN=http://localhost:3000

# Demo user (US-1 Login) — change for real deployments
DEMO_USER_EMAIL=admin@wings.io
DEMO_USER_PASSWORD=Demo123!

# JWT signing — change for real deployments
JWT_SECRET=change-me-in-real-deployments
JWT_EXPIRES_IN=1h
